import * as utils from '../utils'
import * as LinkedList from './a_linkedListNodeClasses'

function shiftLinkedList(head: LinkedList.SLL_Node, k: number): void {
    // a derivation of swapping nodes in LL

  }