/**
 * TEXT JUSTIFICATION - LEET_UBER - EASY
 * 
 */

/**
 * @description 
 * @param words 
 * @param maxWidth 
 * @returns 
 */
function fullJustify(words: string[], maxWidth: number): string[] {
    while(words.reduce((accum, curr) => accum+curr)
    return
};

let test = ['hey', 'dude', 'was', 'on', 'mtv']
console.log(test.reduce((accum, curr) => accum + curr).length)